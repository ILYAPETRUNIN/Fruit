export function pages(state) {
  console.log(state)
  return state.pages;
}

export function contacts(state) {
  return state.contacts;
}

export function footerLinks(state) {
  return state.footerLinks;
}

export function footerLinks1(state) {
  return state.footerLinks1;
}
